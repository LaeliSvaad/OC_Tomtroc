<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirige tout vers index.php sauf si fichier/dossier existe
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [L]
</IfModule>